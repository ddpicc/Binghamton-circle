<view class="filter-bar">
  <view
    class="filter-item {{value === item.value ? 'active' : ''}}"
    wx:for="{{options}}"
    wx:key="value"
    data-value="{{item.value}}"
    bindtap="handleSelect"
  >
    {{item.label}}
  </view>
</view>
