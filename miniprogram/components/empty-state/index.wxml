<view class="empty">
  <view class="empty-icon">😶</view>
  <view class="empty-title">{{title}}</view>
  <view wx:if="{{description}}" class="empty-desc">{{description}}</view>
  <button wx:if="{{actionText}}" class="empty-action" bindtap="handleAction">{{actionText}}</button>
</view>
